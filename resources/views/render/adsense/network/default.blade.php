<iframe id="{{ $data['ne'] }}"
        name="{{ $data['ne'] }}"
        src="{{ $url }}"
        width="{{ $data['width'] }}"
        height="{{ $data['height'] }}"
        scrolling="no"
        frameborder="0"
></iframe>
